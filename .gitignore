# Android build
.gradle/
build/
*/build/
local.properties
.idea/
*.iml
.cxx/
.externalNativeBuild/

# Ignore large model weights (but keep configs)
*.bin
*.safetensors

# Keep model configs
!*.json
!*.params

# Keep runtime .so files (mlc4j needs them)
!*.so

# Cache
.cache/
*.log
